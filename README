这个安卓的小demo, 主要目的就是从服务器上把h264的码流拉下来, 使用mediacodec进行硬件解码.
之前尝试使用jni接收udp数据流, 适合于WiFi内网环境, 但是由于涉及公网推流, 安卓端没有公网ip, 所以只能使用tcp形式
目前的延迟是超过300ms, 明显延迟严重, 看是否能通过降低分辨率来提高速度.
另外, 之所以要使用jni, 是因为学习了ffmpeg库的方式解码, 后来痴迷于硬件解码, 就没用, 所以后来直接换成Java的Socket类来连接.
